(function(define) {
    'use strict';
    define(['backbone'], function(Backbone) {
        var CertificatesModel = Backbone.Model.extend({});
        return CertificatesModel;
    });
}).call(this, define || RequireJS.define);
